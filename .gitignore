# Secrets — never commit
.env

# Cache files ARE committed so GitHub Actions can use them without
# calling stats.nba.com (which blocks CI/CD IP ranges).
# Only exclude the injury cache — it's too volatile to commit.
data/cache/injuries_espn.json

# Python
__pycache__/
*.pyc
*.pyo
.Python

# Logs
logs/

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db
